---
name: forge-session
description: |
  Query the current ContextForge session memory.
  Trigger: /contextforge:forge-session [optional query]
user-invocable: true
---

# Forge Session

Use ContextForge to inspect the current session state.

## Instructions

1. Use the text after the command as the session query. If no text is provided, ask for the current session summary.
2. Call `forge_session`.
3. Present the result as a short, practical session snapshot.
