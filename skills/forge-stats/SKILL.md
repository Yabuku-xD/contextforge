---
name: forge-stats
description: |
  Show ContextForge runtime stats for compression, retrieval, paging,
  and repository state.
  Trigger: /contextforge:forge-stats
user-invocable: true
---

# Forge Stats

Use ContextForge to inspect current repo/session stats.

## Instructions

1. Call `forge_stats`.
2. Present the result in readable markdown.
3. Highlight the most useful signals first, such as repository counts, compression stats, retrieval state, or pager state.
